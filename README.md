# Generic Object Database

A database designed to store different types of objects
without requiring a separate table for every object type.

## Current milestone

Core database schema containing:

- OBJECT_TYPE
- ATTRIBUTE
- OBJECT
- ALLOWED_VALUE
- OBJECT_VALUE

Sample data, relationships, validation logic, and PL/SQL
functionality will be added in later milestones.
